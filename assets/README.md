# Assets

This directory is populated by the scripts, not committed:

- `scripts/fetch-assets.sh` — Firecracker binary + guest kernel (`vmlinux`)
- `scripts/build-rootfs.sh` — Alpine ext4 with python3, nodejs, and `guest-agent`

`configs/firecracker.json` points here.
